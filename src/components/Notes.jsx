function Notes() {
    return (
        <div>
            The given text is not broken into actual tokens but just using a rough estimate of 100 words = 75 tokens.
            Models limits are sometimes given as a "context window" and sometimes as "input" and "output" tokens. Whenever the second approach is used, only the "input" value is used.

        </div>
    )
}

export default Notes
